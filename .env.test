# Non-secret stub values used only for the hermetic Playwright E2E build.
# All requests to this host are intercepted by e2e/fixtures/supabase.ts and
# never leave the browser — this is NOT a real project. Committed on purpose
# (see the !.env.test exception in .gitignore).
VITE_SUPABASE_URL=https://test.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key
